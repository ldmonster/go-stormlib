// Package parity contains the parity test suites that compare go-stormlib
// behavior against an external parity command (Go-backed or StormLib-backed).
//
// The package has no production code; the suites are driven by `go test`
// and read configuration from environment variables. See README.md for the
// contract and the supported variables.
package parity
